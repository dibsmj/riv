import { css } from '@emotion/react';

const RoundButtonStyle = ({ heights }) => css`
  .btntext {
    font-family: 'Chillax Semibold';
    font-style: normal;
    font-weight: 500;
    font-size: 16px;
    line-height: 20px;
    display: flex;
    align-items: center;
    color: #ffffff;
  }
`;

export default RoundButtonStyle;
